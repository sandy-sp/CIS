import streamlit as st


def index_page() -> None:
    st.title("Index")
    st.caption("Embed cleaned chunks into a Qdrant vector database.")
    st.info("Step 3 — Vector Indexing — coming soon. Run 'Clean' first to process pages.")
