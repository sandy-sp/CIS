import streamlit as st


def clean_page() -> None:
    st.title("Clean")
    st.caption("Clean and chunk scraped Markdown files for RAG processing.")
    st.info("Step 2 — Data Cleaning — coming soon. Run 'Scrape' first to collect pages.")
